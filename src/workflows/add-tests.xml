<workflow id="add-tests">
  <name>Test Coverage Expansion</name>
  <trigger>add tests, improve coverage, write tests, missing tests, increase coverage</trigger>
  <description>Add tests to existing code without modifying implementation.</description>
  <mode>sequential</mode>
  <steps>
    <step sequence="1" agent="critic" lens="correctness">
      <goal>Identify untested code paths, edge cases, and boundary conditions.</goal>
      <output>review_report</output>
    </step>
    <step sequence="2" agent="builder" receives="review_report">
      <goal>Write comprehensive test suite covering identified gaps.</goal>
      <output>test_plan, code</output>
    </step>
  </steps>
</workflow>
